Include ~/.ssh/config_neuropoly

Host *
ControlMaster auto
ControlPath ~/.ssh/%r@%h:%p
# HTTP over SSH wants a longer timeout than 3s (I think?)
# This currently helps the connection to data.neuro.polymtl.ca
# ControlPersist 3s